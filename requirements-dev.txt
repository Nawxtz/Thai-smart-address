# ThaiSmartAddress v7.0 — development / test dependencies
# Install with: pip install -r requirements.txt -r requirements-dev.txt
-r requirements.txt

pytest>=8.0.0
pytest-asyncio>=0.23.0
httpx>=0.27.0     # async test client for FastAPI
